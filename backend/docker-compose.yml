services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: eduin_global
    ports:
      - "5432:5432"
    volumes:
      - eduin_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d eduin_global"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  eduin_pgdata:
